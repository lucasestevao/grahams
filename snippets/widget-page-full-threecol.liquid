{% if pages[data].handle != '' %}
<div class="pagesnippetwidget widget sixteen columns">
    <h1><a href="{{ pages[data].url }}">{{ pages[data].title }}</a></h1>
    {% include 'process-html' with pages[data].content %}
</div>
{% endif %}